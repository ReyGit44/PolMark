# Polymarket Parity Arbitrage Bot Configuration
# Copy to .env and fill in your values

# === REQUIRED: Authentication ===
# Your Polygon wallet private key (with 0x prefix)
POLYMARKET_PRIVATE_KEY=0x...

# Your funder address (proxy wallet address from polymarket.com/settings)
POLYMARKET_FUNDER_ADDRESS=0x...

# Signature type: 0=EOA, 1=POLY_PROXY (Magic Link), 2=GNOSIS_SAFE (browser wallet)
POLYMARKET_SIGNATURE_TYPE=2

# === OPTIONAL: Pre-derived API credentials ===
# If not provided, will be derived from private key on startup
# POLYMARKET_API_KEY=
# POLYMARKET_API_SECRET=
# POLYMARKET_API_PASSPHRASE=

# === REQUIRED: Markets to Monitor ===
# Format: condition_id:yes_token_id:no_token_id:tick_size:neg_risk
# Multiple markets separated by commas
# Example for BTC price markets:
POLYMARKET_MARKETS=0xabc123:111222333:444555666:0.01:false

# === Trading Parameters ===
# Minimum edge to execute (default: 0.005 = 0.5%)
MIN_EDGE=0.005

# Slippage buffer (default: 0.002 = 0.2%)
SLIPPAGE_BUFFER=0.002

# Maximum USDC per trade (default: 100)
MAX_NOTIONAL_PER_TRADE=100

# Maximum concurrent paired positions (default: 5)
MAX_OPEN_PAIRS=5

# Cooldown between trades in milliseconds (default: 1000)
COOLDOWN_MS=1000

# === Fee Configuration ===
# Most Polymarket markets have 0 fees
# 15-minute crypto markets have taker fees
MAKER_FEE_BPS=0
TAKER_FEE_BPS=0

# === Risk Management ===
# Maximum daily loss before kill switch (default: 500)
MAX_DAILY_LOSS=500

# Immediate halt threshold (default: 200)
KILL_SWITCH_LOSS_THRESHOLD=200

# === Logging ===
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path
LOG_FILE=arb_bot.log

# === Database ===
# SQLite database path
DB_PATH=arb_bot.db
